{
  "name": "commonapp-celebrate",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rimraf ./public && rimraf ./dist && rimraf ./.cache",
    "watch": "parcel watch src/index.html",
    "bs": "browser-sync start -s 'dist' -f 'dist' --port 9000",
    "dev": "npm run clean && concurrently \"npm run watch\" \"npm run bs\"",
    "build": "parcel build src/index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.13",
    "concurrently": "^5.3.0",
    "parcel-bundler": "^1.12.4",
    "prettier": "^2.1.2",
    "rimraf": "^3.0.2",
    "sass": "^1.28.0"
  }
}
